using UnityEngine;

public class IgniteTest : MonoBehaviour
{
    public EffectContainer target;

    public float power1;
    public float power2;
    public float power3;

   /* void Update()
    {       
        if (Input.GetKeyDown(KeyCode.Space))
        {
            Debug.Log("key");
            EffectInstance effect = new EffectInstance(EffectType.Ignite, power1,target);
            target.ApplyEffect(effect);
        }

        if (Input.GetKeyDown(KeyCode.N))
        {
            EffectInstance effect = new EffectInstance(EffectType.Ignite, power2, target);
            target.ApplyEffect(effect);
        }

        if (Input.GetKeyDown(KeyCode.M))
        {
            EffectInstance effect = new EffectInstance(EffectType.Ignite, power3, target);
            target.ApplyEffect(effect);
        }

        if (Input.GetKeyDown(KeyCode.B))
        {
            EffectInstance effect = new EffectInstance(EffectType.Bleed, 35, target);
            target.ApplyEffect(effect);
        }

        if (Input.GetKeyDown(KeyCode.V))
        {
            EffectInstance effect = new EffectInstance(EffectType.Poison, 5, target);
            target.ApplyEffect(effect);
        }
    }
        */
}
